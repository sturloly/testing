## JS testing demo codes


* default, err message unreadable

  ```
  npm run test
  ```

* without err message, only wrong codes highlighting

  ```
  npm run test -s
  ```

  manifest in package.json : 'test':'mocha || true'
